# ARC.os

A desktop-style workspace for Circle's Arc network: inspect tokens, mint, send in bulk, swap and bridge.

Automated analysis, not investment advice.

## Develop

    npm install
    npm run dev      # http://localhost:3000 — Arc testnet by default
    npm test
